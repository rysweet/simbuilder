# SimBuilder local environment overrides
#
# This file is intentionally minimal so CI and tests are unaffected.
# Add **your own** variables below when running locally, e.g.
#
#   AZURE_TENANT_ID=
#   NEO4J_PASSWORD=
#   AZURE_OPENAI_ENDPOINT=
#   AZURE_OPENAI_KEY=
#
# Never commit secrets – `.env` is git-ignored.
